# 🛍️ Приложение-витрина "Want It - Buy It" (Advertising Stand)

> **Автономная информационная панель** для отображения товаров интернет-магазина с реальным временем обновления контента.

[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.0-green.svg)](https://spring.io/)
[![WebSocket](https://img.shields.io/badge/WebSocket-STOMP-blue.svg)](https://spring.io/guides/gs/messaging-stomp-websocket/)
[![RabbitMQ](https://img.shields.io/badge/RabbitMQ-AMQP-orange.svg)](https://www.rabbitmq.com/)

---

## 🌟 Обзор проекта

**Автономное Spring Boot приложение**, разработанное как информационная панель (витрина), которая в реальном времени отображает актуальный список товаров из основного интернет-магазина.

Приложение предназначено для **быстрого и автоматического обновления контента** без необходимости ручной перезагрузки страницы.

### 🎯 Ключевые особенности:
- **Real-time обновления** через WebSocket
- **Автоматическая синхронизация** с основным магазином
- **Кэширование данных** для высокой производительности
- **UI** на Thymeleaf

---

## 🛠️ Основные технологии

### Backend:
- **Spring Boot** (основной фреймворк)
- **Spring WebFlux** (WebClient для HTTP запросов)
- **Spring AMQP** (интеграция с RabbitMQ)
- **Spring WebSocket** (STOMP для real-time обновлений)

### Frontend:
- **Thymeleaf** (рендеринг шаблонов)
- **WebSocket** (клиентская часть для real-time)

### Хранение данных:
- **Внутренний кэш** (ConcurrentHashMap)
- **In-memory хранилище** для быстрого доступа

  
  ## ⚙️ Архитектура и функциональность

### 1. **Источник данных (Начальная загрузка)**

Приложение является клиентом для основного магазина.

- **Во время запуска** (`ApplicationRunner`) сервис `ProductService` использует `WebClient` для выполнения HTTP-запроса к основному магазину и получения полного списка товаров
- **Данные хранятся в памяти** (`ConcurrentHashMap<String, ProductForStandDto>`) для быстрого доступа

### 2. **Живое обновление (Live Update)**

Приложение подписано на очередь RabbitMQ для получения событий об изменениях товаров.

- **RabbitReceiver**: Получает события (`UPDATE` или `DELETE`) из очереди
- **ProductService**: Обновляет или удаляет товар из внутреннего кэша (`productList`)
- **WebSocket**: После обновления кэша `RabbitReceiver` отправляет сигнал `PRODUCT_UPDATED` по WebSocket-каналу (`/topic/reload`) всем подключенным клиентам
- **Клиент (HTML)**: Фронтенд-страница (`products-for-stand.html`) принимает этот сигнал и выполняет полную перезагрузку (`window.location.reload()`), чтобы получить обновленные данные из кэша

### 3. **Отображение**

- **ProductController**: При запросе `/api/stands/get` достает отфильтрованные списки товаров (**Новые**, **Со скидкой**, **Популярные**) из кэша
- **Thymeleaf**: Рендерит данные в виде трех слайдеров, как определено в шаблоне `products-for-stand.html`

## 🚀 Запуск и конфигурация

- пройдите в основное приложение и протестируйте весь функционал: https://github.com/AlexxCobb/online-store
